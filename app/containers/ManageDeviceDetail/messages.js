/*
 * ManageDeviceDetail Messages
 *
 * This contains all the text for the ManageDeviceDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageDeviceDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageDeviceDetail container!',
  },
});
