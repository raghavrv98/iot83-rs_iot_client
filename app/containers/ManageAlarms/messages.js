/*
 * ManageAlarms Messages
 *
 * This contains all the text for the ManageAlarms container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageAlarms';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageAlarms container!',
  },
});
