/*
 * Pagination Messages
 *
 * This contains all the text for the Pagination component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Pagination';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Pagination component!',
  },
});
