/*
 * NoDataFound Messages
 *
 * This contains all the text for the NoDataFound component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NoDataFound';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NoDataFound component!',
  },
});
