/*
 * SkeletonLoader Messages
 *
 * This contains all the text for the SkeletonLoader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SkeletonLoader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SkeletonLoader component!',
  },
});
