/*
 * LineChart Messages
 *
 * This contains all the text for the LineChart component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LineChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LineChart component!',
  },
});
